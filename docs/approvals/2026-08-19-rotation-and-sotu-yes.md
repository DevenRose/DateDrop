# Approval record — passphrase rotation and SOTU row (Constitution 4.13)

**Date:** 2026-08-19, in the Claude Code session on Lenny
(session `622ff787-503d-4d15-b9ec-7ba03dbf84b9`).

## What Captain was asked

He was told, verbatim summary of the ask: a failed check command printed the gate
passphrase shared by operationrosebush.xyz and the DateDrop editor into the session
transcript, so the phrase was burned; the proposed fix was to run the Rosebush script
`deploy/passphrase.ps1 -Mode new` to generate and set a fresh phrase, copy it to
DateDrop's secret and vault entry the same never-printed way, and check unityrose.org
for the shared phrase. Cost stated: the old passphrase stops working everywhere at that
moment. He was separately shown the exact activity row proposed for
`PAB-Memory:SOTU.md` and asked for the yes Constitution 3.5 requires.

## His answer, verbatim

> "yes on the rotation and SOTU row - finish the work"

(Sent mid-turn on 2026-08-19. Also relevant, sent earlier the same day, mid-turn:
"you have my permission to finish the tasks necessary for the work I requested".)

## What was done on that yes

- New passphrase generated by `X:\Repos\Rosebush\deploy\passphrase.ps1 -Mode new
  -Project rosebush -WordCount 2 -DigitCount 2`; vaulted as `Rosebush:preview`; set as
  `RB_PASSPHRASE` on the Pages project `rosebush`; the site redeployed so the secret
  took effect; login verified working and the gate verified still closed to strangers.
- Same value copied to the vault entry `DateDrop:editor` and the Worker secret
  `DD_PASSPHRASE` over piped input, never printed; login verified working.
- `X:\Repos\UnityRose.org` was searched and contains no passphrase wiring, so there was
  nothing to rotate there.
- The approved activity row was added to `PAB-Memory:SOTU.md` with two phrases updated
  to match reality at write time (the rotation shown as done rather than proposed, and
  the share-icon count at seven), disclosed to Captain in the closing report.
